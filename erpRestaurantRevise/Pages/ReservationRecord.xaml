<Page x:Class="practice.Pages.ReservationRecord"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:practice.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="900" d:DesignWidth="900"
      Title="ReservationRecord">

    <Grid Background="#FF051821">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!-- Page Title -->
        <Label Content="Reservation Records" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" 
               Foreground="White" FontSize="24" FontWeight="Bold"/>

        <!-- Scrollable DataGrid -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Margin="20,10">
            <DataGrid x:Name="RecordsDataGrid" AutoGenerateColumns="False" 
                      HeadersVisibility="Column" IsReadOnly="True"
                      RowHeight="40" FontSize="14"
                      GridLinesVisibility="Horizontal" CanUserResizeColumns="True"
                      MinWidth="800"
                      LoadingRow="RecordsDataGrid_LoadingRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerFullName}" Width="150" />
                    <DataGridTextColumn Header="Date" Binding="{Binding DateReserve, StringFormat=d}" Width="150" />
                    <DataGridTextColumn Header="Time" Binding="{Binding TimeReserve}" Width="150" />
                    <DataGridTextColumn Header="Table" Binding="{Binding TableNumber}" Width="150" />
                    <DataGridTextColumn Header="Guests" Binding="{Binding NumberOfGuests}" Width="120" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</Page>
